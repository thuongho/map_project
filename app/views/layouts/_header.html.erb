<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
       <!-- if signed_in? -->
        
         <!-- @user.username -->
        <!-- else -->
        <%= link_to "MapIt", root_path, id: "logo" %>
        <!-- end -->
       <!-- link_to "Placeholder for logo", '#', id: "logo"  -->
      <nav>
        <ul class="nav pull-right">
          <li><%= link_to "Home",    root_path %></li>
          <li><%= link_to "Tutorial",    '#' %></li>
          <% if signed_in? %>
            <!-- <li> link_to "Users", "#" %></li> -->
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <!-- Account <b class="caret"></b> -->
                <%= current_user.type_name %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <% if current_user.guest? %>
                  <li><%= link_to "Save Account", signup_path %></li>
                <% end %>
                <li class="divider"></li>
                <li>
                  <% if current_user.guest? %>
                  <%= link_to "Sign out", signout_path, method: "delete", data: { confirm: "Signing out without creating account will delete all unsaved cities." } %>
                  <% else %>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                  <% end %>
                </li>
              </ul>
            </li>
          <% else %> 
            <li><%= link_to "Sign in", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>